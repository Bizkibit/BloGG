<h1>New post page</h1>

<% if @post.errors.present? %>
  <%= @post.errors.full_messages.join(', ') %>
<% end %>

<%= form_for @post do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit %>

<% end %>
